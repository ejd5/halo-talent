// ─── Component tests — Halo Companion ───────────

import { describe, it, expect } from "vitest";

describe("Side panel components", () => {
  it("should render without crashing", () => {
    // React component tests require a DOM environment (jsdom or browser)
    // These will be run via WXT's test runner in a browser context
    expect(true).toBe(true);
  });
});
